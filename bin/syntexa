#!/usr/bin/env bash
# Syntexa CLI - Wrapper for Symfony Console
# This script delegates to bin/syntexa.php for better command handling

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"

# Delegate to PHP Symfony Console
php "${ROOT_DIR}/bin/syntexa.php" "$@"
